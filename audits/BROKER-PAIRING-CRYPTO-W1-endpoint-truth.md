# BROKER-PAIRING-CRYPTO-W1 — Endpoint-Truth (Plan-Mode R0)

**Wave:** Phase A — Gemini × Kraken × Alpaca pairing tutorials (Tier-2 Bulk-Spec, 4 chapters).
**Target ICP:** T2 (crypto algo-traders) + T3 (agent builders).
**Probed:** 2026-06-05 (re-verify of Cowork's 2026-06-04 primary-source fetch).
**Fictional primitives:** 0.
**Architect status:** Plan-Mode HALT on the `src/` firewall contradiction → **Mr.1 AUTHORIZED the narrow `src/` relaxation 2026-06-05** (permit ONLY `src/index.ts` route allow-list + `src/lib/integrations-data/exchange-kits.ts` card data; `scripts/` already in scope. MCP-runtime / adapters / tools / x402 / manifests / versions remain hard-firewalled). 🛑 EQUITIES firewall (`equit|stock|robinhood|get_equity` = 0) intact.

## Repos / clean baseline
| Repo | Path | HEAD | Baseline |
|---|---|---|---|
| crypto-quant-signal-mcp (landing render) | `/Users/tank/code/crypto-quant-signal-mcp` | `d4bd5f7` (v1.20.0) | clean — only pre-existing untracked `audits/*` |
| algovault-skills (sources + demos) | `/Users/tank/git/algovault-skills` | `ae51b30` | clean — only pre-existing untracked `audits/*` |

## Primitive truth table (claim → reality → resolution)
| # | Claim | Reality (live probe) | Resolution |
|---|---|---|---|
| R0a | exchange page source + renderer | `algovault-skills/docs/integrations/<slug>.md` → `scripts/render-integrations.mjs` (hardcoded `EXCHANGES`/`FRAMEWORKS`/`MCP_CLIENTS` + `DISPLAY_NAMES`; default `--source ~/git/algovault-skills`) → `landing/integrations/<slug>.html`; applies shared `htmlShell`. Cross-repo render edge ALREADY EXISTS. | add `gemini/kraken/alpaca` to `EXCHANGES` + `DISPLAY_NAMES` (scripts/, in-scope) |
| R0b | "Connect Your Exchange Kit" index marker | `landing/integrations.html` §3 `id=connect-exchange-kit`; cards between `<!-- BUILD:INTEGRATIONS_INDEX_EXCHANGE_KITS:start/end -->`; GENERATED by `build_landing.mjs` from `dist/lib/integrations-data/exchange-kits.js`; CI-gated by `build_landing.mjs --check` (deploy.yml:50, exit 1 on drift) | edit `src/lib/integrations-data/exchange-kits.ts` (+3 entries +footerLinks) then `npm run build:landing` to regen the HTML — both committed together (AUTHORIZED) |
| R0c | stats-banner injection | `htmlShell` injects `quotable-fact` + closing snapshot blocks w/ `data-tr-field` spans (frozen `SNAPSHOT_*` fallbacks 89.4%/56,375/16, date 2026-04-26) + runtime `/js/track-record-proxy.js` hydration. `snapshot-landing-data.mjs` `apply_to_files` EXCLUDES `landing/integrations/*` | new pages inherit automatically via `htmlShell`; ZERO hand-typed track-record numbers; mirror bybit's `data-tr-field` blocks. R4d e2e greps the marker (`data-tr-field`/`AlgoVault × `), not a literal |
| R0d | `_shared/algovault-helper.mjs` | exists; `getAlgoVaultVerdict({coin,timeframe,exchange?='HL',includeReasoning?}) → {signal,confidence,regime,factors,_algovault,_raw}`; free 20 calls/day/IP; surfaces HL-429 verbatim | read-only import (firewalled MAY-NOT-modify) |
| Route | `/integrations/<slug>` live (AC#2) | `src/index.ts:989` hardcoded allow-list `INTEGRATION_EXCHANGES=['binance','okx','bybit','bitget']`; `:1010` returns 404 for unlisted slug. Dockerfile `COPY landing/integrations/` (dir glob → new files auto-ship; NO Dockerfile edit). Caddy routes `/integrations/*` → Express. (types.ts "reads from hasDedicatedPage" comment is STALE — allow-list is hardcoded.) | add `gemini/kraken/alpaca` to `INTEGRATION_EXCHANGES` at `src/index.ts:989` (AUTHORIZED). Precedent: commit `7757cc8` made the identical edit for MCP-clients |
| C1 | Gemini sandbox public ticker | `GET https://api.sandbox.gemini.com/v1/pubticker/btcusd` → **200** `{bid,ask,last,volume}`; v2 `/v2/ticker/BTCUSD` → 200; repo `github.com/gemini/developer-platform` → 200 | demo keyless probe = v1 `/v1/pubticker/btcusd` |
| C2 | Kraken install + "151 commands" | installer URL → **200** (redirects to release-assets); repo `krakenfx/kraken-cli` → 200; **kraken.com/kraken-cli states "151 commands" verbatim** (WebFetch 2026-06-05); `kraken mcp -s all` + keyless paper confirmed | "151 commands" Numerical-citation OK; 134 is stale |
| C2 | Kraken keyless demo path | `kraken` CLI ABSENT locally → demo uses not-installed branch; `https://api.kraken.com/0/public/Ticker?pair=XBTUSD` → **200** JSON (AC1 REST fallback live) | demo: preflight `command -v kraken` → install pointer + keyless `kraken ticker` equivalent via documented REST when CLI absent (relaxation logged below) |
| C3 | Alpaca `uvx alpaca-mcp-server` v2.0.2 | PyPI `.info.version` = **2.0.2** (matches); `uvx` present | `uvx alpaca-mcp-server` per spec |
| C3 | Alpaca paper host | `https://paper-api.alpaca.markets/v2/orders` → **401** (live, keys-gated); `api.alpaca.markets/v2/assets?asset_class=crypto` → 401 | demo keyless branch SKIPs; paper path hard-guards host == `paper-api.alpaca.markets` |
| R4b | track-token `int-claude-desktop` | `docs/integrations/mcp-clients/claude-desktop.md:22` wires `"--header","X-AlgoVault-Track-Token:int-claude-desktop"` in the `mcp-remote` JSON snippet (kept on free tier); server regex `/^[A-Za-z0-9_-]{8,64}$/` | new pages' AlgoVault install snippet uses `mcp-remote` + `X-AlgoVault-Track-Token:int-{gemini,kraken,alpaca}` (all 10 chars, pass). Does NOT backfill existing 8 |
| tooling | node/claude/uv/uvx | all present (`/opt/homebrew/bin/*`); `kraken` absent (expected) | — |

## Identifier diff (R-section ↔ AC ↔ verification gate)
Slugs `gemini`/`kraken`/`alpaca`; track-tokens `int-gemini`/`int-kraken`/`int-alpaca`; `utm_campaign=integration-<venue>`; `examples/<venue>/`; hosts `api.sandbox.gemini.com`, `paper-api.alpaca.markets`, `api.algovault.com/mcp` — all internally consistent R1–R4 ↔ AC1–5 ↔ gate (`integration-gemini|integration-kraken|integration-alpaca`, `examples/gemini examples/kraken examples/alpaca`). No mismatches.

## File-touch list
**algovault-skills:** `docs/integrations/{gemini,kraken,alpaca}.md` (NEW sources) · `examples/{gemini,kraken,alpaca}/{demo.mjs,README.md}` (NEW demos). Read-only import of `examples/_shared/algovault-helper.mjs`.
**crypto-quant-signal-mcp:** `scripts/render-integrations.mjs` (EXCHANGES + DISPLAY_NAMES) · `landing/integrations/{gemini,kraken,alpaca}.html` (rendered) · `src/index.ts:989` (allow-list, AUTHORIZED) · `src/lib/integrations-data/exchange-kits.ts` (+3 entries, AUTHORIZED) · `landing/integrations.html` (regenerated by `build:landing`). NO Dockerfile/deploy.yml edit (dir-glob COPY + `audits/**` paths-ignore).

## Documented relaxations
1. **Kraken AC1 real-`paper buy`**: `kraken` CLI not installable in the demo-author env (no brew formula; installer is a release binary). Demo's keyless path = not-installed branch + live `kraken ticker` REST equivalent (`api.kraken.com/0/public/Ticker`, probed 200). Real `kraken paper buy` fill is best-effort in CI where the installer succeeds; fact-honest fallback per AC1's own clause.
2. **`SNAPSHOT_*` fallback staleness** (89.4%/56,375/16 in `render-integrations.mjs`): pre-existing, shared by ALL 13 existing integration pages, runtime-hydrated by the proxy. NOT this wave's defect; not in scope.

## system-map
Additive on the EXISTING cross-repo render edge + EXISTING `index.ts` route allow-list + EXISTING `exchange-kits` card data. No NEW edge shape (matches spec Map-Anchor "NONE"). Exchange-render count 4→7. `system-map.md updated:` will be recorded in status.md per final finding (likely `n-a — additive render-list/allow-list, no new edge`).
