# AI-CRAWLER-ACCESS-W2 — endpoint-truth (Plan-Mode probe)

**Probed:** 2026-06-03 (UTC) · **Repo:** `crypto-quant-signal-mcp` @ `5196cf7` (== origin/main, clean baseline) · **Probe type:** read-only pre-remediation
**Verdict:** 🟢 **Proceed with R2–R5** (meta, IndexNow, sitemap, presence probe — all unblocked). 🔴 **R1 (www→apex) BLOCKED on a missing credential** — no live `CLOUDFLARE_API_TOKEN` exists locally or on the host (April token expired 2026-04-30). R1 ships as `MANUAL_PENDING` + a `docs/SUBMIT_WWW_REDIRECT.md` runbook unless Mr.1 provisions a token.

## Probe table — `claim | reality | resolution`

| # | Spec claim / anchor | Probed reality | Resolution |
|---|---|---|---|
| 1 | W1's Cloudflare Managed robots.txt is now OFF; committed allow-all authoritative | LIVE `robots.txt` = **156 lines == committed** (re-fetched). **No `Cloudflare Managed` block, no `ai-train=no`, `Google-Extended → Allow: /`.** | ✅ **Verified** — Mr.1's fix confirmed. W1 surprise #1 resolved. |
| 2 | Cloudflare token has `Zone.DNS:Edit`; probe scope, add `Ruleset:Edit` for the redirect | **No `CLOUDFLARE_API_TOKEN` anywhere** — not in `~/.config/algovault/` (only `ADMIN_KEY`), not in repo `.env`, not in `gh secret list`, not in Hetzner `/opt/.../.env`, not in keychain. Vault history: token is **on-demand, provisioned per-task by Mr.1** (April one at `/tmp/cf-blog-mapping/token.env`, scope `DNS:Edit+Zone:Read`, **TTL 2026-04-30 → expired**). | 🔴 **R1 BLOCKED.** Cannot verify scope or call the DNS/Ruleset API with no token. Need Mr.1 to provision `CLOUDFLARE_API_TOKEN` (`Zone.DNS:Edit` + `Zone.Zone:Read` + ideally `Zone.Ruleset:Edit`) for `algovault.com`. Ship `docs/SUBMIT_WWW_REDIRECT.md` + `MANUAL_PENDING`. |
| 3 | `www.algovault.com` → 522 (diagnose DNS record, repoint/redirect) | Still **522** (re-confirmed). `dig` → www + apex both resolve to the same **proxied Cloudflare IPs** (`104.21.64.189`, `172.67.187.95`) → www is an orange-cloud record with no origin server-block (Caddy serves apex only). Apex canonical clean (`https://algovault.com`, no www refs). | 🔴 Fix is an **edge `301 www→apex` Single Redirect** (best — never hits the broken origin) — needs `Ruleset:Edit`. DNS read/edit also needs a token. Both gated on probe #2. |
| 4 | Meta render source per page (mirror stale; grep static-vs-generated) | `/skills`, `/how-it-works`, `/faq`, `/integrations` = static `landing/<p>.html`. **`/track-record` = function-rendered**: `app.get('/track-record')` → `getPerformanceDashboardHtml({isPublic:true})` @ `src/index.ts:2329` (`<title>` @ 2338, **no `<meta name="description">`**); Caddy `handle /track-record { reverse_proxy localhost:3000 }` → **edit TS + rebuild + redeploy container.** `integrations.html` also receives `build_landing.mjs` BUILD blocks (content sections, NOT the `<head>`). | ✅ Sources mapped. Edit 4 static `<head>`s + the TS template; run `build_landing.mjs --check` after editing `integrations.html`. |
| 5 | Current titles (apply drafted only if missing/>60) | **skills 63 (>60) → apply** `AlgoVault Skills — Agent Skills for Claude Code`. **integrations 74 (>60) → apply** `AlgoVault Integrations — Pair with Any Exchange Kit`. **how-it-works 57, faq ~51, track-record "Live Track Record — AlgoVault Labs" (~34) → all ≤60 + present → LEAVE.** | ✅ Title changes: skills + integrations only. |
| 6 | Descriptions too long / missing (apply approved verbatim) | Current: skills **230c** (+ hardcoded `89.4%+ PFE Win Rate`), how-it-works **200c** (`self-tuning quant ML model`), faq **171c**, integrations **168c** (+ `89%+`), track-record **MISSING**. All replaced by approved **qualitative** copy (138–152c, no hardcoded live numbers → forward-stable). | ✅ Apply approved `<meta name="description">` on all 5. track-record = **add** after `<title>`. |
| 7 | **Snapshot injector collision risk** (deploy runs `snapshot-landing-data.mjs`) | `scripts/snapshot-landing-manifest.json`: every claim targets `data-tr-field="…"` spans or JSON-LD `"value"`/description phrases (`across N verified calls`, `N.N% PFE win rate`). **None target `<title>`/`<meta name="description">`.** My qualitative replacements contain no matching number/phrase. Old skills meta `89.4%+ ... PFE Win Rate` ≠ the case-sensitive `%\s+PFE\s+win\s+rate` pattern → never matched anyway. | ✅ **Safe** — meta edits don't touch any managed literal; zero-match count unchanged (no `≥50%` exit-1 risk). |
| 8 | `og:` tags | skills `og:description` = `…89.4%+ PFE Win Rate…`; integrations `og:description` = `89%+ Merkle-verified…` — **hardcoded WR/accuracy numbers (numerical-citation LAW violation).** | ✅ Align `og:title` where the title changes (skills, integrations) + replace those 2 `og:description`s with the approved qualitative copy (clears the LAW violation). how-it-works/faq `og:` = no hardcoded WR → leave (out of flagged scope). |
| 9 | `deploy.yml` ships new `landing/*.txt` (IndexNow key) | Confirmed `deploy.yml:122` `cp landing/*.txt` (+ `:123` `cp sitemap.xml`). | ✅ IndexNow key file + refreshed sitemap deploy via the existing globs. |
| 10 | Sitemap current page set (drop 404s) | Live-200: `/ /faq /glossary /how-it-works /track-record /docs /skills /verify /terms /privacy /integrations` + 13 integration subpages (`binance bitget bybit okx claude-code claude-desktop cline crewai cursor langchain llamaindex maf smithery`). **404: `/pricing`** (spec page-set wrongly lists it — drop), `/integrations/hyperliquid` (no page — exclude). Keep `/llms.txt`+`/llms-full.txt` (200, agent-discovery; no reduction). | ✅ R4 set = 24 HTML URLs + 2 llms.txt = 26, extensionless canonical, `<lastmod>` each. |
| 11 | R5 probe anchors | `geo-queries.yaml` tiers head/niche/branded (**no `presence`** → R5 adds); `runWeeklyProbe(opts?)` @ `geo-orchestrator.ts:187`; `geo-digest.ts`/`geo-dashboard.ts` exist (no `index-presence` → R5 adds); 4-engine `getRetrievalEngines` @ `llm-provider.ts:740`; 8 `geo-*.test.ts`. | ✅ R5 = clean code addition, reuses `geo_mentions` (no schema change). |

## Decisions recorded
- **R1 → MANUAL_PENDING + token request.** I will write `docs/SUBMIT_WWW_REDIRECT.md` (4-click dashboard steps) and request `CLOUDFLARE_API_TOKEN` from Mr.1. If the token is provided this session, I execute the DNS+redirect live and flip R1 to done.
- **R2 title changes = skills + integrations only** (the two >60). how-it-works/faq/track-record titles ≤60+present → left verbatim. track-record description **added** (was missing).
- **R2 `og:` alignment** limited to title-match (skills, integrations) + the 2 `og:description`s carrying hardcoded WR numbers — justified by the numerical-citation Data-Integrity LAW (precedence #2 > minimal-change guidance); documented for transparency. how-it-works/faq `og:` untouched.
- **Sitemap drops `/pricing`** (404) — the spec's listed page-set is wrong on that one (verified live).
- **Gate note (carried from W1):** the W1 `! grep 'Claude-Web|anthropic-ai'` false-positive doesn't apply to W2's gate, but a **meta-length CI canary** is flagged as a WIS candidate so Bing length flags don't recur.

## Probe commands (reproducible)
```bash
curl -fsS https://algovault.com/robots.txt | grep -c 'Cloudflare Managed'      # → 0 (clean)
grep -rh CLOUDFLARE_API_TOKEN ~/.config/algovault /opt/.../.env ; gh secret list # → ∅ (no token)
curl -sI https://www.algovault.com | head -1                                    # → 522
grep -n "app.get('/track-record'" src/index.ts                                  # → 1467 → getPerformanceDashboardHtml@2329
for p in /faq /glossary /how-it-works /track-record /pricing; do curl -s -o/dev/null -w "%{http_code} $p\n" https://algovault.com$p; done  # 200×4, 404 /pricing
```
