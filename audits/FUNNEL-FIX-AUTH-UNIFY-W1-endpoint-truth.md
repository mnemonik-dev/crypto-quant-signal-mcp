# FUNNEL-FIX-AUTH-UNIFY-W1 — endpoint-truth (Plan-Mode Step 0)

**Probed:** 2026-07-10 · against `origin/main` of `~/code/crypto-quant-signal-mcp` (local `main` is **49 commits behind** origin/main — audited the deployed tree via `git show origin/main:<file>`, NOT the stale working tree). Method note: `git grep <rev>` silently missed dynamic-`import()` + `:provider`-param routes; `git show origin/main:… | grep -n` is authoritative here.

**Verdict:** 0 fictional primitives — every spec-cited primitive CONFIRMED real. The required R1 HALT is a **design-decision** HALT (per spec R1/Execution Plan), not a primitive-correction HALT.

| # | Spec claim / primitive | Probe command | Reality | Resolution |
|---|---|---|---|---|
| 1 | `/signup` render source | `git show origin/main:src/index.ts \| sed -n '1423,1470p'` | **Stripe-Checkout 303-redirect** (`app.get('/signup')` index.ts:1423) — NOT an HTML sign-in surface. Forwards utm→Stripe metadata + records CTA funnel event + `?ref`. | ⚠️ **Q1 decision** — mock says "/signup → new API key + quickstart" but /signup is a paid redirect today. Adopting the component here = a behavior change vs ACTIVATION-PAYWALL-W1. |
| 2 | `/account` render source + identity | `git grep -n accountPageHandler` ; `account-handlers.ts` | Function-rendered: `accountPageHandler`→`getAccountPageHtml()` (`src/lib/account-handlers.ts`), registered index.ts:1570. **Identity = pasted API key** (`api_key` input, `av_live_`/`av_free_`). Tabs: paste-key · recover-key · referrals. | ✅ confirmed. Secondary paths (paste/recover/referrals) live here → relocate under shared component. |
| 3 | `/referral` render source + methods | `referral-pages.ts:315` `renderReferralLandingPage` | Function-rendered: `renderReferralLandingPage()` (`src/lib/referral-pages.ts:315`), index.ts:1586. Public explainer + embedded `renderReferralSignupForm()` (**email**→`/api/signup-email`) + "find your link in your account" (→ paste-key). **No Google/GitHub one-tap.** | ✅ confirmed the gap the wave closes. |
| 4 | `/welcome` render source | `welcome-page.ts` (235 lines) | Function-rendered: `getWelcomePageHtml()` (`src/lib/welcome-page.ts`), index.ts:1539. **Already ≈ the target widget**: Start-free btn (`avStartFree`→`/api/start-free`, L62) + OAuth (Continue with Google/GitHub, gated `.live`, →`/auth/:provider`, L64) + email-capture form (L82-95). Behind `NEW_SIGNUP_ENABLED`. | ✅ **This is the source to EXTRACT** the shared partial from. |
| 5 | Static vs function-rendered | `git ls-tree -r origin/main -- landing/ \| grep -iE signup\|account\|referral\|welcome` | **NONE** — all 4 pages are function-rendered in TS. | ✅ confirmed spec hypothesis; no Caddy-static edit. |
| 6 | OAuth buttons already exist | `git show origin/main:src/lib/auth-providers.ts` (160 lines) | `getAuthProvider(id)` (L147, LIVE iff both `<P>_OAUTH_CLIENT_ID/SECRET`), `StubProvider`/`GoogleProvider`/`GitHubProvider`, `generateOAuthState()`, `safeRedirectPath(raw,'/welcome')`. Routes `GET /auth/:provider` (index.ts:2752) + `/auth/:provider/callback` (2777). Google+GitHub **LIVE** (OPS-OAUTH-APPS-WIRE-W1, callback `https://api.algovault.com/auth/{provider}/callback`). | ✅ reuse verbatim — do NOT rebuild. |
| 7 | email-link / deferred-key | `deferred-signup.ts` (97) + `free-keys-store.ts` (220) | `startFree`/`captureEmail` (`deferred-signup.ts`, DI); `mintEphemeralKey`/`mergeEphemeralIntoEmail`/`FREE_KEY_PREFIX='av_free_'` (`free-keys-store.ts`); `POST /api/start-free` (index.ts:2728); `POST /api/signup-email` (index.ts:2564). | ✅ reuse verbatim. |
| 8 | paste-key / recover-key | `account-handlers.ts` exports | `accountPortalHandler`, `accountRecoverKeyHandler` (`/account/recover-key` index.ts:1572, Resend email), `accountReferralsHandler` — all key-in. | ✅ reuse; relocate as secondary. |
| 9 | referral resolves from ANY identity? | read `accountReferralsHandler` + `/referral` + `/api/referral/code` | **NO — resolved from API key.** `accountReferralsHandler` requires a pasted `av_live_`/`av_free_` key (L323/327). `/api/referral/code` is bot-only (internal-bypass, resolves by TG chatId). OAuth callback + email form each **mint a key** for the verified email. | ⚠️ **Bridge = UI wiring** (thread the post-auth key into the same key→code resolver) — **no schema/identity change**. Low-risk. See Q3. |
| 10 | `UNIFIED_SIGNIN_ENABLED` flag | `git grep UNIFIED_SIGNIN_ENABLED origin/main` | **Does not exist yet.** | ✅ create it, mirroring `isNewSignupEnabled()` (`auth-providers.ts:158`). |
| 11 | `NEW_SIGNUP_ENABLED` (pattern) | `auth-providers.ts:158` | `isNewSignupEnabled()` = `NEW_SIGNUP_ENABLED === '1'\|'true'`. **Currently =1 (flipped LIVE 2026-07-10).** | ✅ mirror the accessor. |
| 12 | entitlement `resolveLicense`→`resolveFromApiKeyAsync` | `license.ts:174` / `:313` | `resolveLicense` (L174, 4-tier gate) → `resolveFromApiKeyAsync` (L313). The entitlement chokepoint. | ✅ **FREEZE** — assert `git diff` empty on `license.ts` + `track-token.ts` (the invariant the prior 3 waves proved). |
| 13 | live paid customer `cus_UepU…` | `git grep cus_UepU origin/main` | tier=`starter`, active. **NOT hardcoded in product code** — only in committed `audits/*` + a PII-leak *guard* test (`subscriber-attribution.test.ts:219` asserts HTML must NOT contain it). | ✅ AC2 test = **live resolve-check** (resolve the paid key → tier before/after), not a fixture. |
| 14 | attribution `?src` first-touch | `attribution-sources.ts` `classifySource`; index.ts:3167 | `classifySource()` reads `req.query.src` at `requestContext.run`; `agent_sessions.first_touch_source` write-once (`COALESCE`). Session id across OAuth redirect = ipHash (stable). | ✅ first-touch stamped on the **initial** hit (pre-OAuth); callback only updates last-touch → survives. R5 test required. |
| 15 | `tools/list`=9 frozen | index.ts:863 `for (const name of allToolNames())` | Tool set DERIVES from `feature-registry.ts` `allToolNames()`. Guard tests exist (`tools-list-event.test.ts`, `feature-registry.test.ts`, `tool-annotations.test.ts`). | ✅ wave touches no registry/tool code → byte-identical trivially; keep the assertion. |
| 16 | pre-existing shared sign-in partial? | `git ls-tree … \| grep -iE signin\|auth-widget\|login` | **None** — the widget markup is inline in `welcome-page.ts`. | ✅ EXTRACT into a new shared partial (e.g. `src/lib/signin-component.ts`); import on the target pages. |

## Identifier diff (R-section vs AC-section) — ALL CONSISTENT
`cus_UepU…` (Guardrails/R4/AC2) · `av_live_`/`av_free_` (Guardrails/R4/AC3) · `UNIFIED_SIGNIN_ENABLED` (Method/R2/R3/Exec) · `NEW_SIGNUP_ENABLED` (Context) · routes `/signup /account /referral /welcome` · `resolveLicense`/`resolveFromApiKeyAsync` (Context/Guardrails) · `?src`/`classifySource` (Context/R4/AC4) · `/auth/{provider}/callback` (Context) · `tools/list`=9 (Guardrails/AC5). **No mismatch.**
